# ---- Go ---------------------------------------------------------------
# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories
# vendor/

# Go workspace file
go.work
go.work.sum

# Build output
dist/
bin/

# ---- Python -----------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.pyo
.venv/
venv/
.uv/
*.egg-info/
.eggs/
*.whl
python/.venv/

# ---- Node (explicitly NOT welcome here) ------------------------------
# No node_modules, no package.json, no pnpm-lock.yaml

# ---- Secrets / env ----------------------------------------------------
.env
.env.*
*.age
*.key
!*.pub

# ---- Editor / IDE -----------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~

# ---- OS ---------------------------------------------------------------
.DS_Store
Thumbs.db

# ---- Build / CI artifacts ---------------------------------------------
*.wasm
!web/*.wasm

# Root-level compiled binaries (built by `make build`, output belongs in bin/)
/bootstrap
/cache-gc
/cluster-bootstrap
/cluster-join
/cluster-label
/cluster-probe
/console
/console-wasm
/dataset-build
/doctor
/drain
/eval-harness
/fedcoord
/gateway
/k8s-trainer
/modelfile-gen
/pipeline
/placeholder
/placer
/powermetrics-exporter
/rag
/rag-eval
/rag-ingest
/rag-reindex
/registry
/repo-sync
/status

# Analysis / stats generator outputs
*-cleanup.json
pre-cleanup.json
post-cleanup.json
*-baseline.json
*-post.json
diff-report*.json
*-duplication*.json

# ---- Cluster state ----------------------------------------------------
cluster/kubeconfig
cluster/*.kubeconfig
