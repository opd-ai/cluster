# cluster/flux-system/kustomization.yaml
#
# Kustomize config for the flux-system overlay.
# Applied automatically by Flux after bootstrap.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-system
resources:
  - gotk-sync.yaml
